export function GroupsChat({ groupId }: { groupId: string }) {
  return (
    <div className="flex-1 w-full h-full bg-white flex flex-col items-center justify-center text-gray-400">
      <span>Área de chat do grupo reservada.</span>
    </div>
  );
}
